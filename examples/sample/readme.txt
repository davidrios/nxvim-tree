nxvim-tree demo workspace
=========================

This little tree exists so the explorer has something to show. Open `src/main.rs`
with <CR>, create a file with `a`, rename with `r`, delete with `d`. Press `/` to
filter, `H` to reveal the dotfile.
